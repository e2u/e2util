# groupedcmd

目前為空套件佔位，尚未匯出 API。命令列執行請用上層 [`e2run`](../)。

Empty package stub with no exported API yet. Use parent [`e2run`](../) for goroutine runners.

```bash
go get github.com/e2u/e2util/e2run/groupedcmd
```
